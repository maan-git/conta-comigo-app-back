setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
run:
  web: gunicorn helpmecorona.wsgi-test --log-file -
release:
  image: web
  command:
    - python manage.py migrate