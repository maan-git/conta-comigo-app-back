setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
run:
  web: gunicorn helpmecorona.wsgi --log-file -
release:
  image: web
  command:
    - python manage.py loaddata help_0001_help_category help_0002_help_request_status help_0003_helping_status
